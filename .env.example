# Backend API URL
# Lokalni dev (Vite dev server, backend pokrenut lokalno): http://localhost:8080
# Docker mod: ostaviti prazno ili izostaviti — nginx proxy hendla /api/ rute
VITE_API_URL=http://localhost:8080/api

# Frontend port u Docker modu (podrazumevano: 3000)
FRONTEND_PORT=3000
